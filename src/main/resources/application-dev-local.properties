### Database Configuration
spring.datasource.url=${FASTFOOD_DATABASE_URL}
spring.datasource.username=${FASTFOOD_DATABASE_USER}
spring.datasource.password=${FASTFOOD_DATABASE_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

### Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.username=team15.8soat@gmail.com
spring.mail.password=${FASTFOOD_MAIL_PASSWORD}
spring.mail.port=587
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true

### Temp Config
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.hikari.allow-pool-suspension=true

### Spring Security
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9191/realms/fastfood
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs
